# Fingerprinted build assets (hashed filenames) — safe to cache immutably.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# SPA shell — always revalidate so deploys pick up new bundles.
/index.html
  Cache-Control: no-cache
